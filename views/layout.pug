
//- ye ek layout type hai ..isko extend krke hum login ,reset vgaireh ka code likhenge

<!DOCTYPE html>
html(lang="en")
    head
        title title
        link(rel='stylesheet', href='//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css')
        script(src='//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js')
        script(src='//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js')
    body
        .navbar.navbar-inverse.navbar-static-top(role='navigation')
            .container
                .navbar-header
                    button.navbar-toggle(type='button',data-toggle='collapse',data-target='.navbar-collapse')
                        span.sr-only Toggle navigation
                        span.icon-bar
                        span.icon-bar
                        span.icon-bar
                    a.navbar-brand(href="/") User Aunthentication
                    .collapse.navbar-collapse
                        ul.nav.navbar-nav
                            li.active
                                a(href="/api/auth")  Home
                            if user
                                li
                                    a(href="/api/auth/logout") logout
                            else
                                li
                                    a(href="/api/auth/login") Login

                                li
                                    a(href="/api/auth/register") register
        .container
            if messages.error
                .alert.alert-danger
                    div=messages.error
            if messages.info
                .alert.alert-info
                    div=messages.info
            if messages.success
                .alert.alert-success
                    div=messages.success

        block content
